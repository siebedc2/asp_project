@model Shelter.shared.Animal

@{
    ViewData["Title"] = Model.Name;
}

<form action="">
    <!-- Show the name -->
    <input type="text" value=@Model.Name>
    <input type="submit" value="Edit">
</form>